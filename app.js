const SUPABASE_URL=(window.SEMSEM_CONFIG&&window.SEMSEM_CONFIG.url)||"";
const SUPABASE_ANON_KEY=(window.SEMSEM_CONFIG&&window.SEMSEM_CONFIG.anonKey)||"";
const sbReady=Boolean(window.supabase&&SUPABASE_URL&&SUPABASE_ANON_KEY);
const sb=sbReady?window.supabase.createClient(SUPABASE_URL,SUPABASE_ANON_KEY,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}}):null;
const START_CASH=500000,DAY_MS=60000,RENOVATION_MS=120000,STAFF_UNLOCK=2000000;
const $=id=>document.getElementById(id);
const money=n=>`${Math.max(0,Math.round(Number(n)||0)).toLocaleString("en-US")} ج.م`;
const num=n=>Math.round(Number(n)||0).toLocaleString("en-US");
const clamp=(x,a,b)=>Math.max(a,Math.min(b,x));
const esc=s=>String(s??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));
const slug=s=>String(s).normalize("NFKD").replace(/[^\p{L}\p{N}\-]+/gu,"-").replace(/-+/g,"-").replace(/^-|-$/g,"").toLowerCase();
const imagePool={
  "شقة":"https://images.unsplash.com/photo-1600607687939-ce8a6c25118c?auto=format&fit=crop&w=900&q=80",
  "دوبلكس":"https://images.unsplash.com/photo-1600585154340-be6161a56a0c?auto=format&fit=crop&w=900&q=80",
  "بنتهاوس":"https://images.unsplash.com/photo-1600047509807-ba8f99d2cdde?auto=format&fit=crop&w=900&q=80",
  "فيلا":"https://images.unsplash.com/photo-1600566753190-17f0baa2a6c3?auto=format&fit=crop&w=900&q=80",
  "قصر":"https://images.unsplash.com/photo-1613490493576-7fde63acd811?auto=format&fit=crop&w=900&q=80",
  "شاليه":"https://images.unsplash.com/photo-1600607687920-4e2a09cf159d?auto=format&fit=crop&w=900&q=80",
  "Beach House":"https://images.unsplash.com/photo-1600607687920-4e2a09cf159d?auto=format&fit=crop&w=900&q=80",
  "محل":"https://images.unsplash.com/photo-1555529669-e69e7aa0ba9a?auto=format&fit=crop&w=900&q=80",
  "default":"https://images.unsplash.com/photo-1600607688969-a5bfcd646154?auto=format&fit=crop&w=900&q=80"
};
const regularAreas=[["شبرا","شعبي",690000],["عين شمس","شعبي",520000],["المطرية","شعبي",560000],["المرج","شعبي",500000],["فيصل","متوسط",780000],["الهرم","متوسط",820000],["مدينة نصر","متوسط",1250000],["مصر الجديدة","فاخر",1850000],["المعادي","فاخر",1550000],["الزمالك","فاخر",3200000],["الدقي","فاخر",2350000],["المهندسين","فاخر",1900000],["العجوزة","فاخر",1800000],["عباس العقاد","متوسط",1350000],["القطامية","فاخر",2300000],["التجمع الخامس","فاخر",2200000],["الشيخ زايد","فاخر",2450000],["6 أكتوبر","متوسط",1050000],["الشروق","متوسط",1200000],["العبور","متوسط",950000],["بدر","متوسط",650000],["حدائق أكتوبر","متوسط",760000],["العاصمة الإدارية","استثماري",1750000],["مدينتي","فاخر",2650000],["الرحاب","فاخر",2500000],["زايد الجديدة","فاخر",3500000],["الإسكندرية","متوسط",950000],["سيدي بشر","متوسط",820000]];
const coastal=[["الساحل الشمالي",3200000,["مارينا 1","مارينا 2","مارينا 3","مارينا 4","مارينا 5","بورتو جولف","ستيلا","مراسي","هاسيندا","أمواج","تلال","فوكا باي","سيزر","غزالة باي","رأس الحكمة"]],["العين السخنة",2400000,["بورتو السخنة","الجلالة","العين باي","لاجونا باي","بالم هيلز العين السخنة","أزها","مورانو","تلال السخنة","لافيستا","أويست"]],["العلمين الجديدة",4100000,["مراسي العلمين","نورث إيدج","ذا جيت","مازارين","ممشى العلمين"]],["الجونة",6200000,["الجونة","سوما باي"]]];
const urbanTypes={شعبي:[["استوديو","1 نوم","55م²",.62],["شقة","2 نوم","90م²",.82],["شقة","3 نوم","125م²",1],["شقة","3 نوم","140م²",1.08],["شقة","2 نوم","80م²",.75],["منزل","3 نوم","150م²",1.18]],متوسط:[["استوديو","1 نوم","60م²",.65],["شقة","2 نوم","95م²",.85],["شقة","3 نوم","130م²",1],["شقة فاخرة","3 نوم","165م²",1.28],["دوبلكس","4 نوم","210م²",1.65],["بنتهاوس","4 نوم","230م²",1.9],["منزل","4 نوم","220م²",1.7]],فاخر:[["شقة فاخرة","3 نوم","170م²",1.35],["دوبلكس","4 نوم","230م²",1.75],["بنتهاوس","4 نوم","280م²",2.2],["فيلا","4 نوم","300م²",2.55],["فيلا","5 نوم","380م²",3.05],["قصر","6 نوم","650م²",4.4],["شقة فاخرة","4 نوم","210م²",1.6]],استثماري:[["شقة","2 نوم","90م²",.8],["شقة فاخرة","3 نوم","150م²",1.25],["دوبلكس","4 نوم","220م²",1.7],["بنتهاوس","4 نوم","280م²",2.15],["فيلا","5 نوم","350م²",2.9],["محل","تجاري","70م²",1.2]]};
const sellers=["عم سيد","الحاج محمود","مدام نهى","أستاذ شريف","عماد","الحاج رضا","مدام داليا","عم حسن","مروان","ياسر"];
const staffRoles=[{role:"Sales Agent",name:"مندوب مبيعات",unlock:2000000,salary:25000,commission:.025,skill:42},{role:"Real Estate Broker",name:"وسيط عقاري",unlock:5000000,salary:45000,commission:.035,skill:58},{role:"Senior Broker",name:"وسيط أول",unlock:10000000,salary:75000,commission:.045,skill:68},{role:"Expert Agent",name:"خبير عقاري",unlock:20000000,salary:120000,commission:.055,skill:78}];
const renovationStages=[{name:"الهيكل والأساسات",costPct:.035,valuePct:.025},{name:"الكهرباء والسباكة",costPct:.045,valuePct:.03},{name:"التشطيبات",costPct:.055,valuePct:.04},{name:"المطبخ والحمامات",costPct:.05,valuePct:.035},{name:"الواجهة والمداخل",costPct:.045,valuePct:.035},{name:"التطوير الفاخر",costPct:.075,valuePct:.055}];
const freshState=()=>({cash:START_CASH,rep:18,level:1,office:1,day:1,owned:[],stats:{deals:0,profit:0,flips:0},loans:[],staff:[],staffOffers:[],notifications:[],lastSeenAt:Date.now(),lastDayAt:Date.now(),filter:"الكل",missions:{firstDeal:false,rich:false,properties:false,regions:false}});
let state=freshState(),user=null,profile=null,signup=false,listings=[],selectedArea=null,leaderboardData=[];
function notify(msg){if(!msg)return;state.notifications=(state.notifications||[]).slice(0,11);state.notifications.unshift(msg);renderActivity();}
function normalizeUsername(s){return s.trim().toLowerCase().replace(/\s+/g,"_")}
function validUsername(s){return /^[\p{L}\p{N}_-]{3,18}$/u.test(s)}
function authEmail(u){return normalizeUsername(u)+"@login.semsarmasr.local"}
function cloneState(){return JSON.parse(JSON.stringify(freshState()))}
function netWorth(){return state.cash+state.owned.reduce((s,p)=>s+Number(p.currentValue||0),0)-state.loans.reduce((s,l)=>s+Number(l.remaining||0),0)}
function slots(){return 2+Number(state.office||1)*2}
function dayKey(){return user?`semsem_clock_${user.id}`:null}
function loadDayClock(){const k=dayKey();if(!k)return;try{const x=JSON.parse(localStorage.getItem(k)||"null");if(x&&Number.isFinite(+x.day)&&Number.isFinite(+x.lastDayAt)){state.day=Math.max(+state.day,+x.day);state.lastDayAt=+x.lastDayAt}}catch{}syncDay();saveDayClock()}
function saveDayClock(){const k=dayKey();if(!k)return;try{localStorage.setItem(k,JSON.stringify({day:+state.day||1,lastDayAt:+state.lastDayAt||Date.now()}))}catch{}}
function syncDay(){const now=Date.now(),last=Number(state.lastDayAt||now);const passed=Math.floor(Math.max(0,now-last)/DAY_MS);if(passed){state.day=Math.max(1,Number(state.day||1)+passed);state.lastDayAt=last+passed*DAY_MS;return true}return false}
function dayNow(){syncDay();return Number(state.day||1)}
function rankName(){return state.level>=12?"بارون العقارات":state.level>=8?"مليونير السوق":state.level>=4?"سمسار تقيل":"سمسار مبتدئ"}
function stableImage(p){return p.image||imagePool[p.type]||imagePool.default}
async function saveCloud(){if(!sbReady||!user||!profile)return false;const payload={id:user.id,username:profile.username,cash:Math.round(state.cash),rep:Math.round(state.rep),level:Math.round(state.level),office:Math.round(state.office),day:Math.round(state.day),owned:state.owned,stats:state.stats,loans:state.loans,staff:state.staff,staff_offers:state.staffOffers,net_worth:Math.round(netWorth()),updated_at:new Date().toISOString()};const {error}=await sb.from("game_profiles").upsert(payload,{onConflict:"id"});if(error){console.error("SAVE",error);return false}return true}
async function loadCloud(){if(!sbReady)throw new Error("Supabase غير متصل. راجع config.js.");const {data,error}=await sb.from("game_profiles").select("username,cash,rep,level,office,day,owned,stats,loans,staff,staff_offers,is_admin").eq("id",user.id).maybeSingle();if(error)throw new Error(error.message);if(!data)throw new Error("الحساب موجود في تسجيل الدخول لكن Game Profile غير موجود.");profile={username:data.username,is_admin:Boolean(data.is_admin)};const base=freshState();state={...base,cash:Number(data.cash??base.cash),rep:Number(data.rep??18),level:Number(data.level??1),office:Number(data.office??1),day:Number(data.day??1),owned:Array.isArray(data.owned)?data.owned:[],stats:{...base.stats,...(data.stats||{})},loans:Array.isArray(data.loans)?data.loans:[],staff:Array.isArray(data.staff)?data.staff:[],staffOffers:Array.isArray(data.staff_offers)?data.staff_offers:[]};state.owned.forEach(p=>{if(!p.image)p.image=stableImage(p)});state.lastDayAt=Date.now();loadDayClock();return true}
async function startGame(){try{await loadCloud()}catch(e){console.error(e);if($("authMsg"))$("authMsg").textContent=e.message;return false}syncDay();showGame();buildListings();render();await leaderboard();return true}
function showGame(){$("authScreen")?.classList.add("hidden");$("gameApp")?.classList.remove("hidden");if($("playerName"))$("playerName").textContent=profile?.username||"لاعب";if($("menuUser"))$("menuUser").textContent=profile?.username||"لاعب";if($("adminTabBtn"))$("adminTabBtn").classList.toggle("hidden",!profile?.is_admin)}
async function auth(){if(!sbReady){$("authMsg").textContent="Supabase غير متصل. راجع config.js.";return}const raw=$("username").value.trim(),pass=$("password").value;if(!validUsername(raw))return $("authMsg").textContent="اسم المستخدم من 3 إلى 18 حرف.";if(pass.length<6)return $("authMsg").textContent="كلمة السر لازم تكون 6 حروف على الأقل.";const username=normalizeUsername(raw);$("authBtn").disabled=true;$("authMsg").textContent="جاري الدخول...";try{if(signup){const {data:a,error:ae}=await sb.rpc("username_available",{requested_username:username});if(ae)throw new Error(ae.message);if(!a){$("authMsg").textContent="اسم المستخدم محجوز.";return}const {data,error}=await sb.auth.signUp({email:authEmail(username),password:pass});if(error)throw new Error(error.message);if(!data.user)throw new Error("تعذر إنشاء الحساب.");user=data.user;profile={username};state=freshState();const {error:pe}=await sb.from("game_profiles").insert({id:user.id,username,cash:START_CASH,rep:18,level:1,office:1,day:1,owned:[],stats:state.stats,loans:[],staff:[],staff_offers:[],net_worth:START_CASH});if(pe){await sb.auth.signOut();throw new Error(pe.message)}if(data.session){await startGame()}else $("authMsg").textContent="الحساب اتعمل. لو تأكيد البريد شغال، اقفله من Supabase → Authentication → Providers → Email."}else{let result=await sb.auth.signInWithPassword({email:authEmail(username),password:pass});if(result.error)throw new Error(result.error.message);user=result.data.user;await startGame()}}catch(e){console.error("AUTH",e);$("authMsg").textContent=e.message||"حصل خطأ أثناء تسجيل الدخول."}finally{$("authBtn").disabled=false}}
async function restoreSession(){if(!sbReady)return;try{const {data}=await sb.auth.getSession();if(data.session?.user){user=data.session.user;await startGame()}}catch(e){console.error("SESSION",e)}}
async function logout(){await saveCloud();if(sbReady)await sb.auth.signOut();location.reload()}
function makeRegularListings(){return regularAreas.flatMap((a,ai)=>Array.from({length:10},(_,i)=>{const pool=urbanTypes[a[1]]||urbanTypes.متوسط,t=pool[(ai*3+i)%pool.length],market=Math.round(a[2]*t[3]*(.86+((i*7)%17)/100)),ask=Math.round(market*(.9+((i*5)%9)/100)),condition=58+((ai*11+i*7)%39);return{id:`${slug(a[0])}-${i+1}`,area:a[0],areaType:a[1],type:t[0],rooms:t[1],size:t[2],ask,marketValue:market,condition,seller:sellers[(ai+i)%sellers.length],image:imagePool[t[0]]||imagePool.default}}))}
function makeCoastalListings(){const out=[];coastal.forEach((c,ci)=>c[2].forEach((v,vi)=>{for(let i=1;i<=10;i++){const base=Math.round(c[1]*(.82+vi*.025)*(1+i*.018));out.push({id:`${slug(c[0])}-${slug(v)}-chalet-${i}`,area:c[0],village:v,areaType:"ساحلي",type:"شاليه",rooms:`${2+i%3} نوم`,size:`${105+i*5}م²`,ask:Math.round(base*(.91+((i+vi)%7)/100)),marketValue:base,condition:65+i%31,seller:sellers[(i+vi)%sellers.length],image:imagePool.شاليه})}for(let i=1;i<=10;i++){const base=Math.round(c[1]*(1.55+vi*.04)*(1+i*.025));out.push({id:`${slug(c[0])}-${slug(v)}-villa-${i}`,area:c[0],village:v,areaType:"ساحلي",type:"فيلا",rooms:`${4+i%2} نوم`,size:`${260+i*12}م²`,ask:Math.round(base*(.92+((i+vi)%6)/100)),marketValue:base,condition:70+i%26,seller:sellers[(i+vi+3)%sellers.length],image:imagePool.فيلا})}}));return out}
function buildListings(){const owned=new Set(state.owned.map(p=>p.id));listings=[...makeRegularListings(),...makeCoastalListings()].filter(p=>!owned.has(p.id))}
function propertyImage(p){return `<img loading="lazy" src="${esc(stableImage(p))}" alt="${esc(p.type)}" onerror="this.style.display='none';this.nextElementSibling.style.display='grid'">`}
function render(){syncDay();$("cash").textContent=money(state.cash);$("netWorth").textContent=money(netWorth());$("rep").textContent=`${num(state.rep)}/100`;$("level").textContent=num(state.level);$("day").textContent=num(dayNow());$("dateText").textContent=`اليوم ${num(dayNow())} • ${formatDayTimer()}`;$("playerName").textContent=profile?.username||"لاعب";$("propertyCount").textContent=num(state.owned.length);$("dealCount")&&($("dealCount").textContent=num(state.stats.deals));$("dayCard")&&($("dayCard").textContent=num(dayNow()));$("staffCount").textContent=num(state.staff.length);$("profitTotal").textContent=money(state.stats.profit||0);$("loanDebt").textContent=money(state.loans.reduce((s,l)=>s+Number(l.remaining||0),0));$("notifBadge")&&( $("notifBadge").textContent="0");renderListings();renderPortfolio();renderCities();renderBank();renderStaff();renderProfile();renderMissions();renderLeaderboard();renderTransactions();updateOfficeUI();}
function formatDayTimer(){const left=DAY_MS-(Date.now()-Number(state.lastDayAt||Date.now()));const sec=Math.max(0,Math.ceil(left/1000));return `التالي خلال ${String(Math.floor(sec/60)).padStart(2,"0")}:${String(sec%60).padStart(2,"0")}`}
function propertyCard(p,owned=false,i=0){return `<article class="property"><div class="pic">${propertyImage(p)}<span class="pic-fallback">⌂</span><small>${esc(p.village||p.area)}</small></div><div class="pbody"><div class="property-title"><h3>${esc(p.type)}</h3><span class="hot">${esc(p.areaType)}</span></div><div class="meta"><span class="tag">${esc(p.rooms)}</span><span class="tag">${esc(p.size)}</span><span class="tag">حالة ${num(p.condition)}%</span></div><div class="price-row"><div><div class="price">${money(owned?p.currentValue:p.ask)}</div><div class="estimate">السوق ${money(p.marketValue)}</div></div>${!owned?`<span class="deal">${Math.max(1,Math.round((p.marketValue-p.ask)/p.marketValue*100))}% تحت السوق</span>`:`<span class="deal">ربح محتمل ${money(p.currentValue-p.buyPrice)}</span>`}</div><div class="actions">${owned?`<button class="secondary-btn" data-action="reno" data-i="${i}">ترميم</button><button class="primary-btn" data-action="sell" data-i="${i}">بيع</button>`:`<button class="secondary-btn" data-action="neg" data-id="${esc(p.id)}">فاوض</button><button class="primary-btn" data-action="buy" data-id="${esc(p.id)}">شراء</button>`}</div>${owned?renderReno(p,i):""}</div></article>`}
function renderListings(){let data=listings;if(selectedArea)data=data.filter(p=>p.area===selectedArea.name&&(!selectedArea.village||p.village===selectedArea.village));if(state.filter!=="الكل")data=data.filter(p=>p.areaType===state.filter);$("marketTitle").textContent=selectedArea?(selectedArea.village?`${selectedArea.village} — ${selectedArea.name}`:selectedArea.name):"سوق العقارات";$("backMarket").classList.toggle("hidden",!selectedArea);$("listings").innerHTML=data.slice(0,80).map(p=>propertyCard(p)).join("")||`<div class="empty">مفيش عقارات هنا بالفلتر الحالي.</div>`}
function renovationInfo(p){const r=p.renovation;if(!r)return{stage:0,active:false,done:false,left:0};if(Number(r.stage)>=renovationStages.length)return{stage:Number(r.stage),active:false,done:true,left:0};const left=Math.max(0,Number(r.endsAt||0)-Date.now());return{stage:Number(r.stage),active:left>0,done:false,left}}
function renovationCost(p,s){return Math.max(25000,Math.round(Number(p.currentValue||0)*renovationStages[s].costPct))}
function renovationGain(p,s){return Math.max(10000,Math.round(Number(p.currentValue||0)*renovationStages[s].valuePct))}
function fmtMs(ms){const s=Math.max(0,Math.ceil(ms/1000));return `${String(Math.floor(s/60)).padStart(2,"0")}:${String(s%60).padStart(2,"0")}`}
function renderReno(p,i){const r=renovationInfo(p);if(r.done)return `<div class="deal-box"><b>اكتمل التطوير</b><br>العقار في أعلى مستوى.</div>`;const st=renovationStages[r.stage],cost=renovationCost(p,r.stage);if(r.active)return `<div class="deal-box"><b>${esc(st.name)}</b><br>متبقي <strong>${fmtMs(r.left)}</strong><br><small>كل مرحلة تستغرق دقيقتين.</small></div>`;return `<div class="deal-box"><b>المرحلة ${r.stage+1}/${renovationStages.length}: ${esc(st.name)}</b><br>التكلفة ${money(cost)} • Skip ${money(Math.ceil(cost*.25))}</div>`}
function openReno(i){const p=state.owned[i];if(!p)return;const r=renovationInfo(p);const st=renovationStages[r.stage];if(r.done)return modal(`<h2>ترميم العقار</h2>${renderReno(p,i)}<button class="primary-btn wide" data-modal-close>إغلاق</button>`);const cost=renovationCost(p,r.stage);const body=r.active?renderReno(p,i):`${renderReno(p,i)}<div class="actions"><button class="primary-btn" data-reno-start="${i}">ابدأ</button><button class="secondary-btn" data-reno-skip="${i}">Skip — ${money(Math.ceil(cost*.25))}</button></div>`;modal(`<h2>ترميم وتطوير</h2><p>${esc(p.type)} • ${esc(p.area)}${p.village?` • ${esc(p.village)}`:""}</p>${body}<button class="primary-btn wide" data-modal-close>إغلاق</button>`)}
async function startReno(i){const p=state.owned[i],r=renovationInfo(p);if(!p||r.active||r.done)return;const cost=renovationCost(p,r.stage);if(state.cash<cost)return notify("الكاش مش كفاية للترميم.");state.cash-=cost;p.renovation={stage:r.stage,startedAt:Date.now(),endsAt:Date.now()+RENOVATION_MS};await saveCloud();openReno(i);render()}
async function skipReno(i){const p=state.owned[i],r=renovationInfo(p);if(!p||r.active||r.done)return;const cost=renovationCost(p,r.stage),skip=Math.ceil(cost*.25);if(state.cash<skip)return notify("الكاش مش كفاية للـSkip.");state.cash-=skip;p.currentValue=Math.round(p.currentValue+renovationGain(p,r.stage));p.renovation={stage:r.stage+1,completed:r.stage+1};await saveCloud();openReno(i);render()}
async function processRenos(){let changed=false;for(const p of state.owned){const r=p.renovation;if(r&&!r.completed&&r.endsAt&&Date.now()>=Number(r.endsAt)){const s=Number(r.stage);p.currentValue=Math.round(p.currentValue+renovationGain(p,s));p.renovation={stage:s+1,completed:s+1};changed=true}}if(changed)await saveCloud();return changed}
function renderPortfolio(){$("portfolioList").innerHTML=state.owned.map((p,i)=>propertyCard(p,true,i)).join("")||`<div class="empty">لسه مفيش عقارات. السوق مستني أول صفقة.</div>`}
function negotiationModal(id){const p=listings.find(x=>x.id===id);if(!p)return;const floor=Math.round(p.marketValue*(.82+Math.min(state.rep,80)/900));const max=Math.max(floor,p.ask);modal(`<h2>التفاوض على ${esc(p.type)}</h2><p>${esc(p.area)}${p.village?` • ${esc(p.village)}`:""}</p><div class="deal-box">سعر الطلب: <b>${money(p.ask)}</b><br>سعر السوق: <b>${money(p.marketValue)}</b><br>أقل سعر منطقي: <b>${money(floor)}</b><br><small>التفاوض محدود بـ3 جولات.</small></div><div class="offer-price" id="negValue">${money(p.ask)}</div><input id="negRange" class="range" type="range" min="${floor}" max="${max}" value="${max}" step="1000"><button class="primary-btn wide" id="sendNeg">إرسال العرض</button>`);$("negRange").oninput=e=>$("negValue").textContent=money(e.target.value);$("sendNeg").onclick=async()=>{const offer=Number($("negRange").value);p.negotiationRounds=Number(p.negotiationRounds||0)+1;const gap=(offer-floor)/Math.max(1,p.ask-floor);const chance=clamp(.18+gap*.58+state.rep/500,0,.78);if(Math.random()<chance){p.ask=Math.round(offer);closeModal();await buy(id)}else{closeModal();notify(p.negotiationRounds>=3?"البائع رفض آخر محاولة.":"البائع رفض العرض. قرّب السعر من السوق.")}await saveCloud();render()}}
async function buy(id){const p=listings.find(x=>x.id===id);if(!p)return;if(state.owned.length>=slots())return notify(`سعة المكتب ${slots()} عقارات. طوّر المكتب.`);if(state.cash<p.ask)return notify("الكاش مش كفاية.");const price=Math.round(p.ask);state.cash-=price;state.owned.push({...p,buyPrice:price,currentValue:Math.max(10000,Math.round(p.marketValue*(p.condition/100))),image:stableImage(p),renovation:null});state.stats.deals++;state.rep=clamp(state.rep+1,0,100);if(state.stats.deals%4===0)state.level++;listings=listings.filter(x=>x.id!==id);await saveCloud();notify(`تم شراء ${p.type} في ${p.village||p.area}.`);render()}
async function sell(i){const p=state.owned[i];if(!p)return;if(renovationInfo(p).active)return notify("العقار تحت الترميم.");const sale=Math.round(p.currentValue),profit=sale-Number(p.buyPrice||0);state.cash+=sale;state.stats.profit+=profit;state.stats.flips++;state.owned.splice(i,1);buildListings();await saveCloud();notify(`تم بيع العقار. الربح ${money(profit)}.`);render()}
function renderCities(){const html=[...regularAreas.map(a=>({name:a[0],tier:a[1],base:a[2]})),...coastal.map(c=>({name:c[0],tier:"ساحلي",base:c[1],coastal:true,villages:c[2]}))].map((a,i)=>`<button class="city" data-city="${i}"><div class="city-top"><span>${a.coastal?"ساحلي":"مصر"}</span><span class="tier">${esc(a.tier)}</span></div><h3>${esc(a.name)}</h3><p>${a.coastal?`${a.villages.length} قرى • 10 فلل + 10 شاليهات لكل قرية`:`10 عقارات متنوعة`}</p><span class="village-enter">فتح المنطقة ←</span></button>`).join("");$("cityGrid").innerHTML=html}
function cityClick(i){const all=[...regularAreas.map(a=>({name:a[0],tier:a[1]})),...coastal.map(c=>({name:c[0],tier:"ساحلي",villages:c[2]}))];const a=all[i];if(!a)return;if(a.villages){modal(`<h2>${esc(a.name)}</h2><p>اختار القرية.</p><div class="village-grid">${a.villages.map(v=>`<button class="village" data-village="${esc(v)}" data-area="${esc(a.name)}"><b>${esc(v)}</b><small>10 فلل + 10 شاليهات</small></button>`).join("")}</div>`)}else{selectedArea={name:a.name};showTab("market");renderListings()}}
function renderStaff(){const nw=netWorth();$("staffList").innerHTML=staffRoles.map((r,i)=>{const s=state.staff.find(x=>x.role===r.role);return `<div class="staff-card"><div class="staff-avatar">${i+1}</div><div><h3>${esc(r.name)}</h3><p>يفتح عند ${money(r.unlock)} • راتب ${money(r.salary)} • عمولة ${(r.commission*100).toFixed(1)}%</p><small>مهارة ${r.skill}%${s?` • ${esc(s.level)}`:""}</small></div>${s?`<button class="secondary-btn" data-staff="${esc(s.id)}">إدارة</button>`:`<button class="primary-btn" data-hire="${esc(r.role)}" ${nw<r.unlock?"disabled":""}>توظيف</button>`}</div>`}).join("")}
async function hireStaff(role){const r=staffRoles.find(x=>x.role===role);if(!r||netWorth()<r.unlock)return notify("الموظف ده لسه مقفول. وصل لصافي الثروة المطلوبة.");if(state.staff.length>=5)return notify("الحد الأقصى 5 موظفين.");state.staff.push({id:crypto.randomUUID?crypto.randomUUID():String(Date.now()),role:r.role,name:r.name,level:"Junior",salary:r.salary,commission:r.commission,skill:r.skill,lastSalaryAt:Date.now(),lastProcessedDay:dayNow()});await saveCloud();notify(`تم توظيف ${r.name}. الموظف هيبدأ يشتغل تلقائيًا من اليوم الجاي.`);render()}
function manageStaff(id){const s=state.staff.find(x=>x.id===id),r=s&&staffRoles.find(x=>x.role===s.role);if(!s||!r)return;modal(`<h2>${esc(s.name)}</h2><p>${esc(s.role)} • ${esc(s.level)}</p><div class="deal-box">راتب ${money(s.salary)} • عمولة ${(s.commission*100).toFixed(1)}%<br>مهارة ${s.skill}%</div><div class="actions"><button class="primary-btn" data-staff-up="${esc(id)}">ترقية</button><button class="secondary-btn" data-staff-fire="${esc(id)}">فصل</button></div>`)}
async function upgradeStaff(id){const s=state.staff.find(x=>x.id===id);if(!s)return;if(s.level==="Junior"){s.level="Intermediate";s.skill+=8;s.salary=Math.round(s.salary*1.25);s.commission+=.005}else if(s.level==="Intermediate"){s.level="Senior";s.skill+=8;s.salary=Math.round(s.salary*1.3);s.commission+=.005}else{s.level="Expert";s.skill=Math.min(95,s.skill+7);s.salary=Math.round(s.salary*1.3);s.commission+=.005}await saveCloud();closeModal();render();notify("تمت ترقية الموظف.")}
async function fireStaff(id){state.staff=state.staff.filter(s=>s.id!==id);await saveCloud();closeModal();render();notify("تم فصل الموظف.")}
async function processStaff(){
  if(!state.staff.length)return false;
  const today=dayNow();
  let changed=false;
  let totalNet=0,worked=0;

  for(const s of state.staff){
    if(!Number.isFinite(Number(s.lastProcessedDay))){
      s.lastProcessedDay=today;
      changed=true;
      continue;
    }
    const last=Math.max(0,Number(s.lastProcessedDay));
    const days=Math.min(30,Math.max(0,today-last));
    if(!days)continue;
    const r=staffRoles.find(x=>x.role===s.role);
    if(!r)continue;

    for(let d=1;d<=days;d++){
      const salary=Math.round(Number(s.salary||r.salary));
      if(state.cash < salary){
        s.lastProcessedDay=last+d;
        changed=true;
        continue;
      }
      const gross=Math.max(40000,Math.round(75000 + Number(s.skill||r.skill)*1500));
      const commission=Math.round(gross*Number(s.commission||r.commission));
      const net=gross-commission-salary;
      state.cash=Math.max(0,Math.round(state.cash+net));
      state.stats.profit=Math.round(Number(state.stats.profit||0)+Math.max(0,net));
      state.stats.deals=Math.round(Number(state.stats.deals||0)+1);
      state.rep=clamp(Number(state.rep||0)+(Number(s.skill||r.skill)>=70?.4:.2),0,100);
      totalNet+=net;
      worked++;
      s.lastProcessedDay=last+d;
      changed=true;
    }
  }

  if(worked)notify(`الموظفين خلصوا ${num(worked)} صفقة تلقائية • صافي دخل ${money(totalNet)}.`);
  return changed;
}
function renderBank(){const debt=state.loans.reduce((s,l)=>s+Number(l.remaining||0),0);$("debtTotal").textContent=money(debt);$("loanCount").textContent=num(state.loans.length);$("bankList").innerHTML=state.loans.map((l,i)=>`<div class="bank-loan"><div><b>${money(l.amount)} قرض</b><small>الفائدة ${l.rate}% • المتبقي ${money(l.remaining)} • القسط ${money(l.installment)}</small></div><button class="secondary-btn" data-loan-pay="${i}">سداد قسط</button></div>`).join("")||`<div class="empty">مفيش قروض حالية.</div>`}
function loanLimit(){return Math.max(0,Math.floor(netWorth()*.28))}
function loanModal(){const limit=loanLimit();modal(`<h2>طلب قرض</h2><p>الحد الحالي يعتمد على صافي ثروتك.</p><div class="deal-box">صافي الثروة: <b>${money(netWorth())}</b><br>الحد الأقصى: <b>${money(limit)}</b></div><input id="loanAmount" class="text-input" type="number" min="10000" max="${limit}" step="10000" value="${Math.min(limit,100000)}"><button class="primary-btn wide" id="takeLoan">استلام القرض</button>`);$("takeLoan").onclick=async()=>{const amount=Math.round(Number($("loanAmount").value));if(amount<10000||amount>limit)return notify("مبلغ القرض خارج الحد المسموح.");const rate=8+Math.min(12,state.level);const total=Math.round(amount*(1+rate/100));const installment=Math.ceil(total/10);state.cash+=amount;state.loans.push({id:String(Date.now()),amount,rate,total,remaining:total,installment,installmentsLeft:10});await saveCloud();closeModal();notify(`تم استلام قرض ${money(amount)}.`);render()}}
async function payLoan(i){const l=state.loans[i];if(!l)return;if(state.cash<l.installment)return notify("الكاش مش كفاية للقسط.");state.cash-=l.installment;l.remaining=Math.max(0,l.remaining-l.installment);l.installmentsLeft=Math.max(0,l.installmentsLeft-1);if(!l.remaining)state.loans.splice(i,1);await saveCloud();render()}
async function leaderboard(){if(!sbReady||!$("lb"))return;$("lb").innerHTML=`<div class="empty">جاري تحميل المتصدرين...</div>`;const {data,error}=await sb.rpc("get_leaderboard");if(error){console.error("LEADERBOARD",error);$("lb").innerHTML=`<div class="empty">تعذر تحميل المتصدرين الآن. جرّب تحديث.</div>`;return}leaderboardData=Array.isArray(data)?data:[];renderLeaderboard()}
function renderLeaderboard(){if(!$("lb")||!leaderboardData.length){if($("lb"))$("lb").innerHTML=`<div class="empty">لا يوجد لاعبون في المتصدرين.</div>`;return}$("lb").innerHTML=leaderboardData.map((p,i)=>`<button class="lb-row ${String(p.username).toLowerCase()===String(profile?.username).toLowerCase()?"me":""}" data-user="${esc(p.username)}"><span class="lb-rank">#${i+1}</span><span class="lb-name">${esc(p.username)}</span><span class="lb-money">${money(p.net_worth)}</span><span class="arrow">‹</span></button>`).join("")}
async function publicProfile(username){if(!sbReady)return;const {data,error}=await sb.rpc("get_public_player",{requested_username:username});if(error||!data?.length)return notify("مش قادر أجيب بروفايل اللاعب دلوقتي.");const p=data[0];modal(`<h2>${esc(p.username)}</h2><div class="profile-public"><div><small>الترتيب</small><b>#${num(p.rank)}</b></div><div><small>صافي الثروة</small><b>${money(p.net_worth)}</b></div><div><small>العقارات</small><b>${num(p.property_count)}</b></div><div><small>إجمالي الربح</small><b>${money(p.total_profit)}</b></div><div><small>الرتبة</small><b>${esc(p.rank_name)}</b></div></div>`)}
async function loadAdminPlayers(){if(!profile?.is_admin)return;const box=$("adminPlayers");if(!box)return;box.innerHTML=`<div class="admin-empty">جاري تحميل اللاعبين...</div>`;const {data,error}=await sb.rpc("admin_list_players");if(error){console.error("ADMIN_LIST",error);box.innerHTML=`<div class="admin-empty">تعذر تحميل اللاعبين: ${esc(error.message)}</div>`;return}const rows=Array.isArray(data)?data:[];adminPlayers=rows;$("adminPlayerCount").textContent=num(rows.length);if(!rows.length){box.innerHTML=`<div class="admin-empty">لا يوجد لاعبين.</div>`;return}box.innerHTML=rows.map(p=>`<div class="admin-player"><div class="player-main"><b>${esc(p.username)}</b><small>${p.is_admin?"ADMIN":"لاعب"}</small></div><div><small>Cash</small><strong>${money(p.cash)}</strong></div><div><small>Net Worth</small><strong>${money(p.net_worth)}</strong></div><div><small>العقارات</small><strong>${num(p.property_count)}</strong></div><div><small>Level</small><strong>${num(p.level)}</strong></div><div class="admin-actions"><button class="secondary-btn" data-admin-money="give" data-admin-id="${esc(p.id)}">إضافة فلوس</button><button class="secondary-btn" data-admin-money="remove" data-admin-id="${esc(p.id)}">سحب فلوس</button><button class="secondary-btn" data-admin-view="${esc(p.username)}">Profile</button><button class="danger-btn" data-admin-reset="${esc(p.id)}">Reset</button></div></div>`).join("")}async function adminMoneyAction(action,id){if(!profile?.is_admin)return;const row=adminPlayers.find(p=>String(p.id)===String(id));if(!row)return;const label=action==='give'?'إضافة':'سحب';const raw=prompt(`${label} مبلغ للاعب ${row.username}:`);if(raw===null)return;const amount=Math.round(Number(raw));if(!Number.isFinite(amount)||amount<=0){alert("اكتب مبلغ صحيح أكبر من صفر.");return;}if(action==='remove'&&!confirm(`تأكيد سحب ${money(amount)} من ${row.username}؟`))return;const rpc=action==='give'?'admin_give_money':'admin_remove_money';const {data,error}=await sb.rpc(rpc,{p_id:id,amount});if(error||data!==true){console.error("ADMIN_MONEY",error);alert(error?.message||"فشلت عملية الفلوس.");return;}await loadAdminPlayers();if(String(id)===String(user?.id)){await loadCloud();render();}else{alert(`تم ${action==='give'?'إضافة':'سحب'} ${money(amount)} بنجاح.`);}}async function adminResetPlayer(id){if(!profile?.is_admin)return;if(!confirm("متأكد إنك عايز تصفر تقدم اللاعب ده؟ الحساب نفسه مش هيتحذف."))return;const {data,error}=await sb.rpc("admin_reset_player",{target_id:id});if(error||data!==true){console.error("ADMIN_RESET",error);alert(error?.message||"فشل تصفير اللاعب.");return}await loadAdminPlayers();if(String(id)===String(user?.id)){await loadCloud();render()}}async function adminResetAll(){if(!profile?.is_admin)return;if(!confirm("تحذير: ده هيصفر تقدم كل اللاعبين. الحسابات نفسها مش هتتمسح."))return;if(!confirm("آخر تأكيد: تصفير كل اللاعبين الآن؟"))return;const {data,error}=await sb.rpc("admin_reset_all_players");if(error){console.error("ADMIN_RESET_ALL",error);alert(error.message||"فشل تصفير اللاعبين.");return}alert(`تم تصفير ${num(data)} لاعب.`);await loadAdminPlayers();await loadCloud();render()}
function renderMissions(){const defs=[["firstDeal","أول صفقة","اشترِ أول عقار",state.stats.deals>=1,10000],["rich","نادي الـ2 مليون","وصل لصافي ثروة 2 مليون",netWorth()>=2000000,50000],["properties","محفظة محترمة","امتلك 5 عقارات",state.owned.length>=5,75000],["regions","مستكشف مصر","امتلك عقارات في 3 مناطق",new Set(state.owned.map(p=>p.area)).size>=3,100000]];$("missionsList")&&($("missionsList").innerHTML=defs.map(d=>`<div class="mission"><div><b>${d[1]}</b><small>${d[2]} • مكافأة ${money(d[4])}</small></div><button class="${state.missions[d[0]]?"secondary-btn":"primary-btn"}" data-mission="${d[0]}" ${(!d[3]||state.missions[d[0]])?"disabled":""}>${state.missions[d[0]]?"تم الاستلام":d[3]?"استلام":"مقفول"}</button></div>`).join(""))}
async function claimMission(key){const defs={firstDeal:[state.stats.deals>=1,10000],rich:[netWorth()>=2000000,50000],properties:[state.owned.length>=5,75000],regions:[new Set(state.owned.map(p=>p.area)).size>=3,100000]};const d=defs[key];if(!d||!d[0]||state.missions[key])return;state.missions[key]=true;state.cash+=d[1];await saveCloud();render();}
function renderProfile(){ $("rankName").textContent=rankName();$("statsText").textContent=`${num(state.stats.deals)} صفقة • ${num(state.stats.flips)} بيع • ربح ${money(state.stats.profit)}`;$("achievements").innerHTML=[[state.stats.deals>=1,"أول صفقة","اشتريت أول عقار"],[state.owned.length>=5,"محفظة محترمة","امتلك 5 عقارات"],[netWorth()>=2000000,"نادي الموظفين","وصلت لـ2 مليون"],[state.loans.length>0,"أول قرض","استخدمت البنك"],[state.level>=10,"مليونير السوق","مستوى 10"]].map(a=>`<div class="achievement ${a[0]?"":"off"}"><b>${a[1]}</b><small>${a[2]}</small></div>`).join("")}
function renderActivity(){if($("activityList"))$("activityList").innerHTML=(state.notifications||[]).slice(0,10).map(x=>`<div class="activity-item">${esc(x)}</div>`).join("")||`<div class="empty">مفيش نشاط لسه.</div>`}
function renderTransactions(){renderActivity()}
function updateOfficeUI(){$("officeLevel").textContent=num(state.office);$("officeSlots").textContent=num(slots());const cost=Math.round(750000*Math.pow(2,state.office-1));$("upgradeBtn").textContent=`تطوير — ${money(cost)}`}
async function upgradeOffice(){const cost=Math.round(750000*Math.pow(2,state.office-1));if(state.cash<cost)return notify("الكاش مش كفاية لتطوير المكتب.");state.cash-=cost;state.office++;await saveCloud();render();notify(`تم تطوير المكتب للمستوى ${state.office}.`)}
function showTab(id){document.querySelectorAll(".tab").forEach(t=>t.classList.toggle("active",t.dataset.tab===id));document.querySelectorAll(".panel").forEach(p=>p.classList.toggle("active",p.id===id));closeMenu();window.scrollTo({top:0,behavior:"smooth"})}
function modal(html){$("modalContent").innerHTML=html;$("modal").classList.remove("hidden")}
function closeModal(){$("modal").classList.add("hidden")}
function openMenu(){$("sideMenu").classList.add("open");$("menuOverlay").classList.add("show");document.body.classList.add("menu-open")}
function closeMenu(){$("sideMenu")?.classList.remove("open");$("menuOverlay")?.classList.remove("show");document.body.classList.remove("menu-open")}
async function processTick(){if(!user)return;const changed=syncDay();const ren=await processRenos();const staffChanged=await processStaff();render();if(changed||ren||staffChanged){saveDayClock();await saveCloud()}}
$("authBtn")?.addEventListener("click",auth);$("loginTab")?.addEventListener("click",()=>{signup=false;$("loginTab").classList.add("active");$("signupTab").classList.remove("active");$("authBtn").textContent="دخول"});$("signupTab")?.addEventListener("click",()=>{signup=true;$("signupTab").classList.add("active");$("loginTab").classList.remove("active");$("authBtn").textContent="إنشاء حساب"});$("logoutBtn")?.addEventListener("click",logout);$("loanBtn2")?.addEventListener("click",loanModal);$("upgradeBtn")?.addEventListener("click",upgradeOffice);$("refreshLb")?.addEventListener("click",leaderboard);$("closeModal")?.addEventListener("click",closeModal);$("modal")?.addEventListener("click",e=>{if(e.target===$("modal")||e.target.matches("[data-modal-close]"))closeModal()});$("menuBtn")?.addEventListener("click",openMenu);$("menuClose")?.addEventListener("click",closeMenu);$("menuOverlay")?.addEventListener("click",closeMenu);document.querySelectorAll(".tab").forEach(t=>t.addEventListener("click",()=>showTab(t.dataset.tab)));$("adminRefresh")?.addEventListener("click",loadAdminPlayers);$("adminResetAll")?.addEventListener("click",adminResetAll);document.querySelectorAll(".filter").forEach(b=>b.addEventListener("click",()=>{state.filter=b.dataset.filter;document.querySelectorAll(".filter").forEach(x=>x.classList.remove("active"));b.classList.add("active");renderListings()}));$("backMarket")?.addEventListener("click",()=>{selectedArea=null;renderListings()});
document.addEventListener("click",async e=>{const a=e.target.closest("[data-action]");if(a){const action=a.dataset.action;if(action==="buy")await buy(a.dataset.id);if(action==="neg")negotiationModal(a.dataset.id);if(action==="sell")await sell(Number(a.dataset.i));if(action==="reno")openReno(Number(a.dataset.i));return}const city=e.target.closest("[data-city]");if(city){cityClick(Number(city.dataset.city));return}const village=e.target.closest("[data-village]");if(village){selectedArea={name:village.dataset.area,village:village.dataset.village};closeModal();showTab("market");renderListings();return}const hire=e.target.closest("[data-hire]");if(hire){await hireStaff(hire.dataset.hire);return}const staff=e.target.closest("[data-staff]");if(staff){manageStaff(staff.dataset.staff);return}const up=e.target.closest("[data-staff-up]");if(up){await upgradeStaff(up.dataset.staffUp);return}const fire=e.target.closest("[data-staff-fire]");if(fire){await fireStaff(fire.dataset.staffFire);return}const lp=e.target.closest("[data-loan-pay]");if(lp){await payLoan(Number(lp.dataset.loanPay));return}const lb=e.target.closest("[data-user]");if(lb){await publicProfile(lb.dataset.user);return}const am=e.target.closest("[data-admin-money]");if(am){await adminMoneyAction(am.dataset.adminMoney,am.dataset.adminId);return}const ar=e.target.closest("[data-admin-reset]");if(ar){await adminResetPlayer(ar.dataset.adminReset);return}const av=e.target.closest("[data-admin-view]");if(av){await publicProfile(av.dataset.adminView);return}const mission=e.target.closest("[data-mission]");if(mission){await claimMission(mission.dataset.mission);return}const rs=e.target.closest("[data-reno-start]");if(rs){await startReno(Number(rs.dataset.renoStart));return}const rk=e.target.closest("[data-reno-skip]");if(rk){await skipReno(Number(rk.dataset.renoSkip));return}});
restoreSession();
setInterval(processTick,1000);setInterval(async()=>{if(!user)return;await saveCloud()},30000);
